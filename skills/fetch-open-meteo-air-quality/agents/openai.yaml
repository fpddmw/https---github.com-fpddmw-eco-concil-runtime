interface:
  display_name: "Open-Meteo Air Quality Fetch"
  short_description: "Fetch hourly air-quality background"
  default_prompt: "Use $fetch-open-meteo-air-quality to fetch modeled CAMS air-quality background for explicit coordinates, dates, domain, and hourly variables. Do not treat modeled fields as station observations or treat missing fields as absence of pollutants without checking domain, variable, and date coverage."
