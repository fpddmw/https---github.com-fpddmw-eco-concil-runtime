interface:
  display_name: "Open-Meteo Flood Fetch"
  short_description: "Fetch historical and forecast flood signals"
  default_prompt: "Use $fetch-open-meteo-flood to fetch Open-Meteo river discharge data for a specified date window and coordinates."
