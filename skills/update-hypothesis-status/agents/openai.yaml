interface:
  display_name: "Update Hypothesis Status"
  short_description: "Create or update an evidence-backed hypothesis card"
  default_prompt: "Use $update-hypothesis-status only when a finding, evidence bundle, proposal, or source evidence ref supports the hypothesis status change. Always pass an evidence_ref or linked_artifact_ref; do not use board notes as canonical judgement."
