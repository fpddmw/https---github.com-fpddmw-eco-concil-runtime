interface:
  display_name: "Eco Lookup Normalized Signal"
  short_description: "Inspect one normalized signal by id"
  default_prompt: "Use $eco-lookup-normalized-signal to inspect one canonical signal from the unified signal plane by signal_id and return compact fields plus provenance, adding raw JSON only when explicitly requested."