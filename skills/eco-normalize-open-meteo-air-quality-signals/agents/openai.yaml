interface:
  display_name: "Eco Normalize Open-Meteo Air Quality Signals"
  short_description: "Normalize Open-Meteo air-quality outputs into unified environment signals"
  default_prompt: "Use $eco-normalize-open-meteo-air-quality-signals to normalize one open-meteo-air-quality-fetch artifact into canonical environment-signal rows in the unified signal plane and return compact receipts, artifact refs, signal ids, and board-ready hints."
