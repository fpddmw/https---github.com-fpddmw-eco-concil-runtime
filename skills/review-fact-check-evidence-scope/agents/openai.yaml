interface:
  display_name: "Review Fact Check Evidence Scope"
  short_description: "Optional-analysis explicit evidence-scope review helper"
  default_prompt: "Use $review-fact-check-evidence-scope only after approval and only with structured verification_scope fields for question, receptor/source scope, windows, lag/spatial rules, required roles/classes, and excluded inferences. Emit caveats, not factual conclusions."
