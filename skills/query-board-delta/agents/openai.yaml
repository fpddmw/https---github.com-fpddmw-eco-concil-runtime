interface:
  display_name: "Eco Read Board Delta"
  short_description: "Read compact round-scoped board deltas"
  default_prompt: "Use $query-board-delta to read a compact round-scoped slice of visible board events, active hypotheses, and challenge tickets. Treat empty or short deltas as board/cursor/round visibility limits, not as evidence absence or readiness."
