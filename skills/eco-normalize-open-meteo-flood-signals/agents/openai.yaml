interface:
  display_name: "Eco Normalize Open-Meteo Flood Signals"
  short_description: "Normalize Open-Meteo flood outputs into unified environment signals"
  default_prompt: "Use $eco-normalize-open-meteo-flood-signals to normalize one open-meteo-flood-fetch artifact into canonical environment-signal rows in the unified signal plane and return compact receipts, artifact refs, signal ids, and board-ready hints."
