interface:
  display_name: "Eco Import Fetch Execution"
  short_description: "Run fetch-plan queue, normalization, and execution receipt components"
  default_prompt: "Use $normalize-fetch-execution to run an approved fetch plan through queue-runner, normalizer-runner, and execution-receipt components. It writes raw artifacts and DB signal rows only; receipt-only or no-row output preserves lineage but is not a downstream analysis, extraction plan, or evidence-absence conclusion."
