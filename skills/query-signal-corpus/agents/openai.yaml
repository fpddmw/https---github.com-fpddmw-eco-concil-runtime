interface:
  display_name: "Eco Query Signal Corpus"
  short_description: "Search archived historical signals across prior runs"
  default_prompt: "Use $query-signal-corpus to search historical signal matches from the shared signal corpus archive."