interface:
  display_name: "Eco Link Claims To Observations"
  short_description: "Legacy optional empirical-link helper"
  default_prompt: "Use $link-claims-to-observations only after a skill approval request has been approved for an explicitly routed empirical observation question. Persist provenance-aware rule traces and do not treat claim-observation matching as the default report basis."
