interface:
  display_name: "Eco Link Claims To Observations"
  short_description: "Deprecated WP4 legacy alias"
  default_prompt: "Do not use $link-claims-to-observations for new work. It now returns a WP4 deprecated-helper stop artifact and points to a future scoped evidence review helper that must be approved and DB-council-cited before report-basis use."
