interface:
  display_name: "GDELT Events Fetch"
  short_description: "Fetch GDELT Events files with retries and logs"
  default_prompt: "Use $fetch-gdelt-events to resolve and fetch row-level GDELT Events exports for a UTC window. Treat this as same-family follow-up to DOC reconnaissance when article search is too narrow; normalize before drawing DB-backed conclusions."
