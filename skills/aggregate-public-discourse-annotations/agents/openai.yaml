interface:
  display_name: "Aggregate Public Discourse Annotations"
  short_description: "Optional-analysis sample-level public discourse annotation aggregation"
  default_prompt: "Use $aggregate-public-discourse-annotations only after approval. Aggregate sample labels with explicit annotation basis; do not infer general public opinion, rank sources, or make report conclusions."
