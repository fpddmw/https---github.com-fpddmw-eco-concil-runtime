interface:
  display_name: "OpenAQ Data Fetch"
  short_description: "Fetch OpenAQ raw metadata, measurements, or archive partitions"
  default_prompt: "Use $fetch-openaq with explicit fetch-metadata, fetch-measurements, or fetch-archive-backfill. Discover metadata before measurements when IDs are uncertain, and do not treat empty measurements as absence of observations without checking IDs, window, and API/archive mode."
