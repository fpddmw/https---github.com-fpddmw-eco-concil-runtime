interface:
  display_name: "Eco Publish Council Decision"
  short_description: "Publish a canonical council decision from the decision draft"
  default_prompt: "Use $publish-council-decision to publish a canonical council decision from the current decision memo draft while enforcing report prerequisites, overwrite guards, and explicit operator approval."
