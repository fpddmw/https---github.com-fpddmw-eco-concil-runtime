interface:
  display_name: "Query Formal Signals"
  short_description: "Query compact formal signals from the unified signal plane"
  default_prompt: "Use $query-formal-signals to query compact formal-signal rows already normalized into the unified signal plane. Use returned item-level evidence_refs and evidence_basis for findings or bundles; empty results can mean DB/filter/round_scope/metadata limits, not absence of formal records."
