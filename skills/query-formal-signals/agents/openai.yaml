interface:
  display_name: "Eco Query Formal Signals"
  short_description: "Query compact formal signals from the unified signal plane"
  default_prompt: "Use $query-formal-signals to query compact formal-signal rows from the unified signal plane with run, round, source, kind, time, docket, agency, submitter, optional typed metadata, and keyword filters. Do not infer missing issue, stance, concern, citation, or route metadata while querying."
