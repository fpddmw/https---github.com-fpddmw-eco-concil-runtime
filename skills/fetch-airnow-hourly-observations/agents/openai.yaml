interface:
  display_name: "AirNow Hourly Obs Fetch"
  short_description: "Fetch AirNow hourly AQ observations"
  default_prompt: "Use $fetch-airnow-hourly-observations to fetch AirNow hourly AQ observations for a UTC window and bbox, then write the full JSON payload to the assigned raw artifact path."
