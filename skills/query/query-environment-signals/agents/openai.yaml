interface:
  display_name: "Query Environment Signals"
  short_description: "Query compact environment signals from the unified signal plane"
  default_prompt: "Use $query-environment-signals to query compact environment-signal rows already normalized into the unified signal plane. Use returned item-level evidence_refs and evidence_basis for findings or bundles; empty results can mean DB/filter/round_scope/normalization limits, not absence of physical conditions."
