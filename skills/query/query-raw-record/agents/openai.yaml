interface:
  display_name: "Query Raw Record"
  short_description: "Inspect one raw record through signal provenance"
  default_prompt: "Use $query-raw-record to inspect one raw source record by signal_id or artifact path and locator, returning the smallest provenance-preserving raw slice plus evidence_refs/evidence_basis. Empty lookup is a provenance/id/artifact visibility issue, not source-family absence."
