interface:
  display_name: "Eco Materialize History Context"
  short_description: "Assemble archived case and signal context for the current round"
  default_prompt: "Use $materialize-history-context to retrieve archived cases and historical signals as compact history context for the current round. Treat history as leads and caveats, not current-run proof, source ranking, or a default plan."
