interface:
  display_name: "Query Public Signals"
  short_description: "Query compact public signals from the unified signal plane"
  default_prompt: "Use $query-public-signals to query compact public-signal rows from the unified signal plane with run, round, source, kind, time, and keyword filters. Use the returned item-level evidence_refs and evidence_basis for findings or evidence bundles; do not infer claims while querying."
