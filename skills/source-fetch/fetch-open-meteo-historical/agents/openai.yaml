interface:
  display_name: "Open-Meteo Historical Fetch"
  short_description: "Fetch historical weather and soil signals"
  default_prompt: "Use $fetch-open-meteo-historical to fetch modeled/reanalysis weather or soil context for explicit coordinates and dates. Keep timezone alignment visible, and do not treat validation failures or sparse modeled rows as absence of weather conditions or as source attribution."
