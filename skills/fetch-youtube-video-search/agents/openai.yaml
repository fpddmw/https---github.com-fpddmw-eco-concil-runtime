interface:
  display_name: "YouTube Video Search"
  short_description: "Search YouTube videos by query and time window"
  default_prompt: "Use $fetch-youtube-video-search to discover candidate YouTube videos for a topic/domain. It does not fetch comment discourse; if discourse semantics matter, select videos and follow with $fetch-youtube-comments."
